local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local MarketplaceService = game:GetService("MarketplaceService")

local player = Players.LocalPlayer

local WEBHOOK_URL = "https://discord.com/api/webhooks/1507724709519818983/08djlL2aoUHTWAchnPGOlSTEQrmU6ymbcwJHMnhGYAQsGU04W9nhFyt0sX7LyFTgKwfx"

local function sendLog()
    if WEBHOOK_URL == "" or WEBHOOK_URL == "YOUR_WEBHOOK_HERE" then
        return
    end

    local gameName = "Unknown"

    pcall(function()
        gameName = MarketplaceService:GetProductInfo(game.PlaceId).Name
    end)

    local payload = {
        embeds = {{
            title = "🛡️ Leo's Hub Premium Logs",
            description = "Activity detected at " .. os.date("%H:%M:%S"),
            color = 65280,

            fields = {
                {
                    name = "👤 Player",
                    value = string.format(
                        "Name: %s\nDisplay Name: %s\nID: %s",
                        player.Name,
                        player.DisplayName,
                        player.UserId
                    ),
                    inline = false
                },

                {
                    name = "🎮 Game",
                    value = string.format(
                        "Name: %s\nPlaceID: %s",
                        gameName,
                        game.PlaceId
                    ),
                    inline = false
                },

                {
                    name = "📜 Script",
                    value = "Leo's Hub Premium",
                    inline = false
                },

                {
                    name = "📊 Status",
                    value = "EXECUTED",
                    inline = false
                },

                {
                    name = "📝 Details",
                    value = "User executed the Leo's Hub Premium script.",
                    inline = false
                }
            },

            footer = {
                text = "Leo's Hub Premium • Security System"
            }
        }}
    }

    local requestFunction =
        request or
        http_request or
        (syn and syn.request)

    if requestFunction then
        pcall(function()
            requestFunction({
                Url = WEBHOOK_URL,
                Method = "POST",
                Headers = {
                    ["Content-Type"] = "application/json"
                },
                Body = HttpService:JSONEncode(payload)
            })
        end)
    end
end

sendLog()

loadstring(game:HttpGet("https://encrypt-x.pages.dev/Scripts?Id=Premium"))("Premium")
